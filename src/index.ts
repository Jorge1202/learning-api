/**
 * Entrada principal del API REST.
 * Bootstrap de Express + montaje de rutas + jobs nocturnos.
 */

// TODO: import express from "express";
// TODO: import cors from "cors";
// TODO: import { schedule } from "node-cron";

// const app = express();
// app.use(cors());
// app.use(express.json());

// app.use("/auth", authRoutes);
// app.use("/onboarding", onboardingRoutes);
// app.use("/graph", graphRoutes);
// ...

// Job nocturno: comparar Grafo Maestro con instancias de usuario activas
// schedule("0 3 * * *", async () => { /* versioning.service */ });

// const PORT = Number(process.env.PORT ?? 4000);
// app.listen(PORT, () => console.log(`API arriba en :${PORT}`));

export {}; // placeholder
